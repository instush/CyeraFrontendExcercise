# Base URL of the backend API.
# Local dev can omit this (defaults to http://localhost:3000).
# In production (e.g. Netlify), set it to your deployed backend URL.
VITE_API_BASE_URL=http://localhost:3000
